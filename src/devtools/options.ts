export interface MountKweriDevToolsOptions {
  position?: 'bottom-right' | 'bottom-left' | 'left' | 'right';
  /** Label shown in the instance switcher. Defaults to the instance's baseURL. */
  label?: string;
}
